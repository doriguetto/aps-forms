Prompts:

- /memories/init-memories

- @.cursor/rules/core/cursor-rules.mdc List all source files and folders in the project, and create a new cursor rule outlining the directory structure and important files and folders

- @.cursor/rules/core/cursor-rules.mdc @package.json Analyise all major dependencies and create a cursor rule outlining the stack of the application and the version and any remarks on best practices on those versions

- @.cursor/rules/core/cursor-rules.mdc @cards I want to generete 2 cursor rule for the attached folder. It contains two block types:
  core/franklin/components/block/v1/block and core/franklin/components/block/v1/block/item. Analyse it carefuly and outline all the convensions found. output as 2 rules inside an "eds" folder one for each block type
